{
	"name": "new-tab",
	"description": "Open a note of your choice when creating a new tab, like in the browser.",
	"author": "legitimate1",
	"version": "0.11.11",
	"keywords": [],
	"license": "MIT",
	"main": "main.js",
	"scripts": {
		"build": "node .esbuild.mjs",
		"build:prod": "node .esbuild.mjs production",
		"release:patch": "pwsh -NoProfile -File release.ps1 patch",
		"release:minor": "pwsh -NoProfile -File release.ps1 minor",
		"release:major": "pwsh -NoProfile -File release.ps1 major"
	},
	"devDependencies": {
		"@types/node": "^22.5.5",
		"builtin-modules": "^3.2.0",
		"esbuild": "^0.24.0",
		"obsidian": "latest",
		"tslib": "2.7.0",
		"typescript": "^5.6.2"
	}
}
